# PR #13 — chore(ai-bots): add GitHub PR workflow, merge agents and Jira automation scripts

**Autor:** Wandersonla
**Branch:** `chore/ai-bots-agents` → `main`
**URL:** https://github.com/Wandersonla/xpe/pull/13
**Coletado em:** 2026-03-29 15:55
**⚠️ Diff truncado**

---

## Diff

```diff
diff --git a/ai-bots/create_aicma_tasks.py b/ai-bots/create_aicma_tasks.py
new file mode 100644
index 0000000..08d4998
--- /dev/null
+++ b/ai-bots/create_aicma_tasks.py
@@ -0,0 +1,225 @@
+#!/usr/bin/env python3
+"""Cria Tarefas AICMA no Jira para cada US do backlog v1/07-jira-backlog-mvp.md"""
+
+import json, base64, urllib.request, urllib.error, os
+
+BASE_URL = os.environ.get("JIRA_BASE_URL", "https://bit4devs.atlassian.net")
+PROJECT  = os.environ.get("JIRA_PROJECT", "BIT")
+EMAIL    = os.environ.get("JIRA_EMAIL", "")
+TOKEN    = os.environ.get("JIRA_TOKEN", "")
+
+AUTH = base64.b64encode(f"{EMAIL}:{TOKEN}".encode()).decode()
+HEADERS = {"Authorization": f"Basic {AUTH}", "Content-Type": "application/json", "Accept": "application/json"}
+
+# ── ADF helpers ───────────────────────────────────────────────────────────────
+
+def h2(text): return {"type":"heading","attrs":{"level":2},"content":[{"type":"text","text":text}]}
+def para(text): return {"type":"paragraph","content":[{"type":"text","text":text}]}
+def bullet_list(items): return {"type":"bulletList","content":[{"type":"listItem","content":[{"type":"paragraph","content":[{"type":"text","text":i}]}]} for i in items]}
+def ordered_list(items): return {"type":"orderedList","content":[{"type":"listItem","content":[{"type":"paragraph","content":[{"type":"text","text":i}]}]} for i in items]}
+def code_block(text): return {"type":"codeBlock","attrs":{},"content":[{"type":"text","text":text}]}
+
+def adf(*nodes): return {"version":1,"type":"doc","content":list(nodes)}
+
+def build_description(t):
+    return adf(
+        h2("Intenção (I)"),    para(t["intencao"]),
+        h2("Conteúdo (C)"),    bullet_list(t["conteudo"]),
+        h2("Motivo (M)"),      para(t["motivo"]),
+        h2("Ação (A)"),        bullet_list(t["acoes"]),
+        h2("Requisitos Funcionais (RF)"),     bullet_list(t["rf"]),
+        h2("Requisitos Não Funcionais (RNF)"),bullet_list(t["rnf"]),
+        h2("Notas Técnicas"),  bullet_list(t["notas"]),
+        h2("Critérios de Aceite"), bullet_list(t["criterios"]),
+        h2("Steps de Implementação"), ordered_list(t["steps"]),
+        h2("Sugestão de PR"),  code_block(t["pr"]),
+        h2("Commit semântico sugerido"), code_block(t["commit"]),
+    )
+
+# ── Criar issue ───────────────────────────────────────────────────────────────
+
+def create_task(t, epic_key):
+    summary = f"[{t['tag']}] {t['title']}"
+    body = {"fields": {
+        "project": {"key": PROJECT},
+        "summary": summary,
+        "issuetype": {"name": "Tarefa"},
+        "parent": {"key": epic_key},
+        "description": build_description(t),
+    }}
+    data = json.dumps(body).encode()
+    req  = urllib.request.Request(f"{BASE_URL}/rest/api/3/issue", data=data, headers=HEADERS, method="POST")
+    try:
+        with urllib.request.urlopen(req) as r:
+            res = json.loads(r.read())
+            key = res["key"]
+            print(f"  ✓  {key}  {BASE_URL}/browse/{key}  — {summary}")
+            return key
+    except urllib.error.HTTPError as e:
+        print(f"  ✗  ERRO {e.code}: {e.read().decode()[:200]}")
+        return None
+
+# ── Tasks AICMA ───────────────────────────────────────────────────────────────
+
+TASKS = [
+
+# ─── US-001 ──────────────────────────────────────────────────────────────────
+{"tag":"NEXORA-Foundation","title":"Bootstrapar API NestJS com módulos base",
+ "intencao":"Configurar o repositório da API NestJS com estrutura de módulos, ferramentas de qualidade de código e hooks de commit, garantindo uma base sólida e padronizada para todo o desenvolvimento futuro.",
+ "conteudo":["Inicializar projeto NestJS com CLI oficial","Configurar ESLint e Prettier com regras do projeto","Configurar Husky e lint-staged para hooks de commit","Configurar commitlint com Conventional Commits","Configurar Swagger/OpenAPI no módulo principal","Definir estrutura base de módulos (shared, modules, health)","Validar que o projeto compila sem erros"],
+ "motivo":"Sem uma base estruturada e com ferramentas de qualidade configuradas desde o início, o time acumula dívida técnica rapidamente e perde consistência entre contribuições, tornando difícil a manutenção e revisão de código.",
+ "acoes":["Executar nest new com configurações iniciais","Instalar e configurar ESLint, Prettier e suas integrações","Instalar Husky e configurar hooks pre-commit e commit-msg","Instalar commitlint e criar commitlint.config.js","Configurar SwaggerModule no AppModule","Criar estrutura de pastas conforme v1/05-mvc-ddd-e-estrutura.md","Executar npm run build e validar saída em dist/"],
+ "rf":["RF01: O projeto deve compilar com tsc sem erros","RF02: ESLint deve executar sem warnings em zero-tolerance","RF03: Prettier deve formatar o código automaticamente","RF04: Commits devem ser bloqueados se não seguirem Conventional Commits","RF05: Swagger deve estar acessível em /api","RF06: Estrutura de pastas deve seguir o padrão documentado em v1/05"],
+ "rnf":["RNF01: O build deve completar em menos de 30 segundos","RNF02: Nenhuma dependência de desenvolvimento deve ir para dependências de produção","RNF03: O projeto deve rodar com Node.js 24 LTS"],
+ "notas":["Verificar compatibilidade entre versões de @nestjs/core e @nestjs/swagger","Usar tsconfig strict mode conforme padrão do projeto","O Husky requer git init antes da configuração","Lint-staged deve rodar apenas nos arquivos staged, não em todo o projeto"],
+ "criterios":["Projeto compila com npm run build sem erros ou warnings","npm run lint retorna exit 0","Commit com mensagem inválida é bloqueado pelo hook","Swagger acessível em http://localhost:3000/api","Estrutura de pastas reflete o padrão definido na documentação"],
+ "steps":["1. Executar: nest new nexora-academy-api --package-manager npm","2. Instalar dependências de qualidade: ESLint, Prettier, Husky, commitlint","3. Criar .eslintrc.js e .prettierrc com as configurações do projeto","4. Executar: npx husky install e criar hooks pre-commit e commit-msg","5. Criar commitlint.config.js com extends: ['@commitlint/config-conventional']","6. Instalar @nestjs/swagger e configurar SwaggerModule em main.ts","7. Criar estrutura de pastas: src/shared/, src/modules/, src/health/","8. Executar npm run build e validar dist/","9. Fazer commit inicial: chore: bootstrap nestjs project with base structure"],
+ "pr":"[NEXORA-Foundation] Bootstrap NestJS com estrutura base, lint e commit hooks",
+ "commit":"chore(bootstrap): configura nestjs com eslint, prettier, husky e swagger",
+ "epic":"BIT-1"},
+
+# ─── US-002 ──────────────────────────────────────────────────────────────────
+{"tag":"NEXORA-IAM","title":"Integrar autenticação Keycloak com guards e papéis",
+ "intencao":"Integrar o Keycloak como provedor de identidade na API NestJS, validando tokens JWT, mapeando papéis para guards e protegendo endpoints conforme perfil do usuário autenticado.",
+ "conteudo":["Configurar módulo de autenticação com estratégia JWT Passport","Integrar validação de token com Keycloak (JWKS ou introspection)","Criar guard global de autenticação (JwtAuthGuard)","Criar guard de autorização por papéis (RolesGuard)","Criar decoradores @Roles e @Public","Mapear papéis do Keycloak (realm_access.roles) para guards","Testar fluxo completo com token válido e inválido"],
+ "motivo":"Sem autenticação integrada, qualquer endpoint da API fica exposto. A ausência de validação de token e papéis compromete a segurança do sistema e inviabiliza o controle de acesso por perfil.",
+ "acoes":["Instalar @nestjs/passport, passport-jwt e jwks-rsa","Criar AuthModule com JwtStrategy e PassportModule","Criar JwtAuthGuard como guard global na aplicação","Criar RolesGuard para verificar papéis do payload JWT","Criar decorator @Roles(...roles) e @Public()","Configurar KEYCLOAK_JWKS_URI no env e usar jwks-rsa para validação","Adicionar testes unitários para os guards"],
+ "rf":["RF01: Endpoints sem @Public() devem retornar 401 sem token válido","RF02: Token expirado deve retornar 401","RF03: Usuário sem papel adequado deve retornar 403","RF04: Papéis devem ser lidos de realm_access.roles no payload JWT","RF05: @Public() deve permitir acesso sem autenticação"],
+ "rnf":["RNF01: A validação do token não deve adicionar mais de 50ms de latência","RNF02: As chaves JWKS devem ser cacheadas e não buscadas a cada request","RNF03: Credenciais do Keycloak não devem ser hardcoded"],
+ "notas":["Usar jwks-rsa com cache habilitado para buscar as public keys","O campo de papéis no JWT do Keycloak está em realm_access.roles","Verificar se o ambiente local tem o Keycloak rodando via docker-compose.dev.yml","Testar com os usuários pré-configurados: admin@nexora.com, teacher@nexora.com"],
+ "criterios":["Request sem Authorization header retorna 401","Request com token expirado retorna 401","Request com papel incorreto retorna 403","Request com papel correto retorna 200","@Public() em endpoint de health retorna 200 sem token"],
+ "steps":["1. Instalar: @nestjs/passport passport passport-jwt jwks-rsa","2. Criar src/shared/auth/auth.module.ts com imports necessários","3. Criar JwtStrategy usando jwks-rsa para validação das chaves","4. Criar JwtAuthGuard estendendo AuthGuard('jwt')","5. Registrar JwtAuthGuard como guard global em AppModule","6. Criar RolesGuard verificando Reflector e realm_access.roles","7. Criar decoradores @Roles e @Public com SetMetadata","8. Testar com curl usando token gerado pelo Keycloak local","9. Adicionar testes unitários para JwtAuthGuard e RolesGuard"],
+ "pr":"[NEXORA-IAM] Integra autenticação Keycloak com JWT guards e decoradores de papéis",
+ "commit":"feat(auth): integra keycloak jwt com guards de autenticacao e autorizacao",
+ "epic":"BIT-2"},
+
+# ─── US-003 ──────────────────────────────────────────────────────────────────
+{"tag":"NEXORA-Users","title":"Implementar endpoints de busca de usuários (ID, nome, count)",
+ "intencao":"Expor endpoints REST para consulta de perfis de usuário por ID e por nome, além de um endpoint de contagem total, permitindo que o time de atendimento localize usuários rapidamente.",
+ "conteudo":["Criar UsersModule com controller, service e repository","Implementar GET /users/:id com validação de MongoId","Implementar GET /users/name/:name com busca case-insensitive","Implementar GET /users/count retornando total de registros","Proteger endpoints com JwtAuthGuard e papel adequado","Documentar todos os endpoints no Swagger","Escrever testes unitários e de integração"],
+ "motivo":"Sem endpoints de busca, o time de atendimento não consegue localizar usuários para responder chamados, dependendo de acesso direto ao banco de dados, o que é inseguro e ineficiente.",
+ "acoes":["Criar estrutura MVC+DDD do módulo users (presentation/application/domain/infrastructure)","Implementar UserRepository com MongoDB","Criar UsersController com os 3 endpoints","Adicionar pipe MongoIdValidation para :id","Adicionar @ApiTags e @ApiOperation no Swagger","Criar testes unitários para UsersService","Criar teste de integração para os endpoints"],
+ "rf":["RF01: GET /users/:id deve retornar 200 com o usuário ou 404","RF02: GET /users/name/:name deve suportar busca parcial case-insensitive","RF03: GET /users/count deve retornar { total: number }","RF04: ID inválido (não MongoId) deve retornar 400","RF05: Endpoints devem exigir autenticação JWT"],
+ "rnf":["RNF01: Busca por nome deve usar índice MongoDB para performance","RNF02: Resposta de todos os endpoints deve ser < 200ms em p95","RNF03: Swagger deve estar atualizado com exemplos de resposta"],
+ "notas":["Usar regex case-insensitive no MongoDB para busca por nome","Criar índice em { name: 1 } na collection users","Não expor campos sensíveis como password ou tokens nas respostas","Validar ObjectId com class-validator IsMongoId"],
+ "criterios":["GET /users/:id retorna usuário correto ou 404","GET /users/name/marina retorna usuários cujo nome contém 'marina'","GET /users/count retorna objeto com campo total","ID inválido retorna 400 com mensagem clara","Swagger documenta os 3 endpoints com exemplos"],
+ "steps":["1. Criar estrutura de pastas: src/modules/users/{presentation,application,domain,infrastructure}","2. Criar User entity e UserRepository com MongooseModel","3. Criar UsersService com métodos findById, findByName, count","4. Criar UsersController com os 3 endpoints e decoradores Swagger","5. Adicionar MongoIdPipe para validação de :id","6. Registrar UsersModule no AppModule","7. Escrever testes unitários para UsersService","8. Escrever teste de integração com supertest","9. Validar documentação Swagger em /api"],
+ "pr":"[NEXORA-Users] Adiciona endpoints GET /users/:id, /users/name/:name e /users/count",
+ "commit":"feat(users): adiciona endpoints de busca por id, nome e contagem",
+ "epic":"BIT-3"},
+
+# ─── US-004 ──────────────────────────────────────────────────────────────────
+{"tag":"NEXORA-Catalog","title":"Implementar CRUD de cursos com publicação",
+ "intencao":"Criar o módulo de catálogo com CRUD completo de cursos, incluindo a ação de publicação que muda o status do curso para disponível na plataforma.",
+ "conteudo":["Criar CoursesModule com CRUD completo","Implementar POST /courses, GET /courses, GET /courses/:id, PATCH /courses/:id, DELETE /courses/:id","Implementar PATCH /courses/:id/publish para publicação","Proteger rotas com papéis (admin para escrita, autenticado para leitura)","Documentar no Swagger com DTOs","Escrever testes unitários e de integração"],
+ "motivo":"Sem o catálogo de cursos, nenhuma outra funcionalidade da plataforma pode ser utilizada — turmas, inscrições e o produto educacional em si dependem da existência de cursos cadastrados.",
+ "acoes":["Criar estrutura MVC+DDD do módulo courses","Definir Course entity com campos: title, description, status (draft/published), createdAt","Criar CreateCourseDto e UpdateCourseDto com validações","Implementar CoursesService com métodos CRUD e publish","Criar CoursesController com todos os endpoints","Adicionar @Roles('admin') para operações de escrita","Documentar DTOs e respostas no Swagger"],
+ "rf":["RF01: POST /courses deve criar curso com status 'draft'","RF02: PATCH /courses/:id/publish deve mudar status para 'published'","RF03: DELETE /courses/:id deve retornar 204","RF04: GET /courses deve suportar paginação com limit e offset","RF05: Apenas admins podem criar, atualizar, publicar e deletar cursos"],
+ "rnf":["RNF01: Swagger deve documentar todos os DTOs com exemplos","RNF02: Campos obrigatórios devem ser validados com class-validator","RNF03: Respostas devem seguir envelope padrão da API"],
+ "notas":["Status inicial do curso deve ser 'draft' — nunca 'published' automaticamente","Publicação deve ser uma ação explícita e separada do PATCH genérico","Considerar soft delete ou hard delete conforme ADR do projeto","Adicionar índice em { status: 1 } para queries de listagem"],
+ "criterios":["POST /courses cria curso com status draft","PATCH /courses/:id/publish muda status para published","GET /courses retorna lista paginada","DELETE /courses/:id retorna 204","Endpoint de publicação exige papel admin"],
+ "steps":["1. Criar estrutura de pastas src/modules/courses/","2. Criar Course schema/entity com status enum (draft, published)","3. Criar CreateCourseDto e UpdateCourseDto com class-validator","4. Implementar CoursesRepository com MongoDB","5. Implementar CoursesService com create, findAll, findById, update, publish, remove","6. Criar CoursesController com endpoints e decoradores Swagger","7. Proteger rotas de escrita com @Roles('admin')","8. Escrever testes unitários para CoursesService","9. Escrever testes de integração para os endpoints CRUD"],
+ "pr":"[NEXORA-Catalog] Adiciona CRUD completo de cursos com ação de publicação",
+ "commit":"feat(courses): implementa crud de cursos com status draft e published",
+ "epic":"BIT-4"},
+
+# ─── US-005 ──────────────────────────────────────────────────────────────────
+{"tag":"NEXORA-Classrooms","title":"Implementar CRUD de turmas com janelas de inscrição e capacidade",
+ "intencao":"Criar o módulo de turmas permitindo abertura de turmas vinculadas a cursos existentes, com registro de janelas de inscrição, capacidade máxima e agenda de aulas.",
+ "conteudo":["Criar ClassroomsModule com CRUD completo","Validar courseId existente ao criar turma","Registrar janelas de inscrição (enrollmentStart, enrollmentEnd)","Registrar capacidade máxima e agenda","Proteger com papéis (admin para escrita)","Documentar no Swagger","Escrever testes"],
+ "motivo":"Sem turmas, alunos não têm onde se inscrever. A ausência de validação de courseId e janelas de inscrição pode gerar dados inconsistentes e impedir o fluxo correto de inscrições.",
+ "acoes":["Criar Classroom entity com courseId, capacity, enrollmentWindow e schedule","Validar existência do courseId no CoursesService ao criar turma","Implementar ClassroomsService com CRUD","Criar ClassroomsController com endpoints","Adicionar validações de datas nas janelas de inscrição","Proteger rotas de escrita com @Roles('admin')","Escrever testes unitários e de integração"],
+ "rf":["RF01: POST /classrooms deve exigir courseId válido (404 se não existir)","RF02: Turma deve registrar enrollmentStart e enrollmentEnd","RF03: Turma deve registrar capacity (número inteiro positivo)","RF04: GET /classrooms/:id deve retornar dados completos da turma","RF05: Apenas admins podem criar e atualizar turmas"],
+ "rnf":["RNF01: Criação de turma com courseId inexistente deve retornar 404 com mensagem clara","RNF02: Datas de inscrição devem ser validadas (end > start)","RNF03: Swagger deve documentar o schema completo de Classroom"],
+ "notas":["Usar referência por ID para courseId — não embutir o curso na turma","enrollmentEnd deve ser obrigatoriamente posterior a enrollmentStart","Capacity deve ser um inteiro positivo maior que zero","Considerar índice em { courseId: 1 } para queries de turmas por curso"],
+ "criterios":["POST /classrooms com courseId inexistente retorna 404","Turma criada contém enrollmentStart, enrollmentEnd e capacity","GET /classrooms/:id retorna dados completos","PATCH /classrooms/:id atualiza campos individuais","Rota de criação exige papel admin"],
+ "steps":["1. Criar estrutura src/modules/classrooms/","2. Criar Classroom schema com courseId, capacity, enrollmentWindow, schedule","3. Criar CreateClassroomDto com validações de datas e capacity","4. Implementar ClassroomsRepository","5. Implementar ClassroomsService com validação de courseId via CoursesService","6. Criar ClassroomsController com CRUD","7. Proteger rotas de escrita com @Roles('admin')","8. Escrever testes unitários para ClassroomsService","9. Escrever teste de integração para criação com courseId inválido"],
+ "pr":"[NEXORA-Classrooms] Adiciona CRUD de turmas com validação de curso, janelas e capacidade",
+ "commit":"feat(classrooms): implementa crud de turmas com janelas de inscricao e capacidade",
+ "epic":"BIT-5"},
+
+# ─── US-006 ──────────────────────────────────────────────────────────────────
+{"tag":"NEXORA-Classrooms","title":"Vincular professor a turma com auditoria",
+ "intencao":"Implementar endpoint dedicado para alocação de professor em turma, garantindo que o professor passe a visualizar a turma em seus acessos e que a ação seja registrada em log de auditoria.",
+ "conteudo":["Criar endpoint POST /classrooms/:id/teachers com teacherId no body","Validar existência do professor (userId com papel teacher)","Associar professor à turma no banco","Registrar evento de auditoria com userId, teacherId e timestamp","Garantir que professor autenticado veja apenas suas turmas","Documentar no Swagger"],
+ "motivo":"Sem vinculação explícita de professor, não há como controlar quais turmas cada professor acompanha, impossibilitando o filtro de acesso e o rastreio de responsabilidades acadêmicas.",
+ "acoes":["Adicionar campo teacherIds no schema de Classroom","Criar AllocateTeacherDto com teacherId validado","Implementar método allocateTeacher no ClassroomsService","Criar endpoint POST /classrooms/:id/teachers no ClassroomsController","Emitir log estruturado com Winston ao alocar professor","Adicionar filtro por professor no GET /classrooms para papel teacher"],
+ "rf":["RF01: POST /classrooms/:id/teachers deve associar teacherId à turma","RF02: Professor alocado deve ver a turma em GET /classrooms","RF03: Alocação deve gerar log de auditoria com userId e timestamp","RF04: teacherId deve corresponder a usuário com papel 'teacher'","RF05: Apenas admin pode alocar professores"],
+ "rnf":["RNF01: Log de auditoria deve ser estruturado (JSON) e incluir correlationId","RNF02: A alocação deve ser idempotente (alocar o mesmo professor duas vezes não gera duplicata)","RNF03: Endpoint deve retornar 200 com a turma atualizada"],
+ "notas":["Usar $addToSet no MongoDB para garantir idempotência na alocação","O filtro de GET /classrooms para professores deve usar o userId do token JWT","Log de auditoria deve ir para o logger da aplicação, não para uma collection separada neste momento","Validar que teacherId é um MongoId válido antes de checar no banco"],
+ "criterios":["POST /classrooms/:id/teachers retorna 200 com turma atualizada","Professor alocado aparece em teacherIds da turma","GET /classrooms com papel teacher retorna apenas turmas do professor","Alocação duplicada não cria entrada duplicada em teacherIds","Log estruturado é emitido com userId, teacherId e classroomId"],
+ "steps":["1. Adicionar campo teacherIds: string[] ao Classroom schema","2. Criar AllocateTeacherDto com @IsMongoId() teacherId","3. Implementar allocateTeacher no ClassroomsService com $addToSet","4. Criar endpoint POST /classrooms/:id/teachers no controller","5. Adicionar lógica de filtro em findAll: se papel é teacher, filtrar por teacherIds","6. Emitir log estruturado no service após alocação","7. Proteger endpoint com @Roles('admin')","8. Escrever teste unitário para idempotência da alocação","9. Escrever teste de integração para o endpoint"],
+ "pr":"[NEXORA-Classrooms] Adiciona endpoint de alocação de professor com auditoria",
+ "commit":"feat(classrooms): adiciona alocacao de professor com log de auditoria",
+ "epic":"BIT-5"},
+
+# ─── US-007 ──────────────────────────────────────────────────────────────────
+{"tag":"NEXORA-Enrollments","title":"Implementar inscrição de aluno em turma aberta",
+ "intencao":"Permitir que alunos autenticados se inscrevam em turmas abertas, garantindo validação de janela de inscrição, respeito à capacidade e prevenção de inscrições duplicadas.",
+ "conteudo":["Criar EnrollmentsModule com endpoint POST /enrollments","Validar janela de inscrição aberta (data atual entre enrollmentStart e enrollmentEnd)","Validar capacidade disponível na turma","Impedir inscrição duplicada do mesmo aluno na mesma turma","Retornar 409 para duplicata e 422 para violações de regra","Documentar no Swagger","Escrever testes de regras de negócio"],
+ "motivo":"Sem validações de janela e capacidade, alunos podem se inscrever fora do período permitido ou além do limite de vagas, gerando inconsistência operacional e conflitos na gestão de turmas.",
+ "acoes":["Criar Enrollment entity com studentId, classroomId e enrolledAt","Verificar se data atual está dentro da janela de inscrição","Contar inscrições ativas e comparar com capacity da turma","Verificar inscrição duplicada com findOne({studentId, classroomId})","Retornar erros semânticos corretos (409, 422)","Criar EnrollmentsController e EnrollmentsService","Escrever testes unitários para cada regra de negócio"],
+ "rf":["RF01: POST /enrollments deve criar inscrição se janela estiver aberta","RF02: Inscrição fora da janela deve retornar 422 com mensagem clara","RF03: Inscrição com turma lotada deve retornar 422","RF04: Inscrição duplicada deve retornar 409","RF05: studentId deve ser extraído do token JWT (não do body)"],
+ "rnf":["RNF01: As 3 validações devem ser atômicas para evitar race conditions em cenários de alta concorrência","RNF02: Resposta de erro deve incluir código de erro legível (ex: ENROLLMENT_WINDOW_CLOSED)","RNF03: Índice único em { studentId, classroomId } deve prevenir duplicatas no nível do banco"],
+ "notas":["Criar índice único no MongoDB em { studentId: 1, classroomId: 1 }","studentId deve vir do payload JWT, não do body da requisição","Para race conditions de capacidade, considerar transação MongoDB ou campo de contador com $inc","Testar os três cenários de erro explicitamente nos testes de integração"],
+ "criterios":["POST /enrollments com janela aberta e vaga disponível retorna 201","POST /enrollments fora da janela retorna 422","POST /enrollments com turma lotada retorna 422","POST /enrollments duplicado retorna 409","studentId é sempre o do token — body não aceita studentId"],
+ "steps":["1. Criar estrutura src/modules/enrollments/","2. Criar Enrollment schema com studentId, classroomId, enrolledAt e índice único","3. Criar EnrollDTO com apenas classroomId (studentId vem do JWT)","4. Implementar EnrollmentsService com validação de janela, capacidade e duplicata","5. Criar EnrollmentsController com POST /enrollments","6. Proteger endpoint com @Roles('student')","7. Escrever testes unitários para cada regra de negócio (3 cenários de erro + sucesso)","8. Escrever testes de integração com supertest","9. Validar índice único no MongoDB com teste de inscrição duplicada"],
+ "pr":"[NEXORA-Enrollments] Adiciona inscrição de aluno com validação de janela, capacidade e duplicata",
+ "commit":"feat(enrollments): implementa inscricao com validacao de janela e capacidade",
+ "epic":"BIT-6"},
+
+# ─── US-008 ──────────────────────────────────────────────────────────────────
+{"tag":"NEXORA-Enrollments","title":"Listar turmas e alunos inscritos para professor",
+ "intencao":"Permitir que professores autenticados visualizem suas turmas e a lista de alunos inscritos em cada turma, exibindo apenas os dados mínimos necessários para acompanhamento acadêmico.",
+ "conteudo":["Criar GET /classrooms para professor retornar apenas suas turmas","Criar GET /classrooms/:id/enrollments para listar alunos inscritos","Retornar dados mínimos do aluno (id, nome)","Proteger com papel teacher","Documentar no Swagger"],
+ "motivo":"Sem visibilidade sobre seus alunos, professores não conseguem acompanhar a operação, tomar decisões pedagógicas ou verificar presença, o que compromete a gestão acadêmica da plataforma.",
+ "acoes":["Adicionar filtro por teacherId no GET /classrooms (já iniciado na US-006)","Criar endpoint GET /classrooms/:id/enrollments no ClassroomsController","Fazer populate/join de studentId para retornar dados mínimos do aluno","Restringir acesso ao endpoint para papel teacher","Garantir que professor só veja alunos de suas próprias turmas"],
+ "rf":["RF01: GET /classrooms com papel teacher retorna apenas turmas do professor autenticado","RF02: GET /classrooms/:id/enrollments retorna lista de alunos inscritos","RF03: Cada aluno na lista deve ter ao menos id e name","RF04: Professor não pode ver alunos de turmas de outros professores","RF05: Endpoint exige papel teacher ou admin"],
+ "rnf":["RNF01: A query de alunos deve usar projeção MongoDB para retornar apenas campos necessários","RNF02: Resposta deve ser paginável com limit e offset","RNF03: Swagger deve documentar o schema de resposta com campos mínimos"],
+ "notas":["Usar populate do Mongoose ou lookup do MongoDB para trazer dados do aluno","Garantir que a verificação de posse da turma seja feita no service, não apenas no controller","Dados mínimos: { id, name } — não expor email, senha ou documentos","Considerar criar um DTO de resposta StudentSummaryDto"],
+ "criterios":["Professor vê apenas suas turmas em GET /classrooms","GET /classrooms/:id/enrollments retorna alunos com id e name","Professor não consegue acessar alunos de turma alheia (403)","Resposta aceita parâmetros de paginação","Swagger documenta o schema de resposta"],
+ "steps":["1. Confirmar que GET /classrooms já filtra por teacherIds (US-006)","2. Criar GET /classrooms/:id/enrollments no ClassroomsController","3. Implementar getEnrollments no ClassroomsService com verificação de posse","4. Fazer lookup/populate de studentId retornando apenas { _id, name }","5. Criar StudentSummaryDto com id e name","6. Proteger endpoint com @Roles('teacher', 'admin')","7. Adicionar paginação com limit e offset","8. Escrever teste unitário para verificação de posse","9. Escrever teste de integração para o endpoint"],
+ "pr":"[NEXORA-Enrollments] Adiciona visibilidade de turmas e alunos para professor",
+ "commit":"feat(classrooms): adiciona listagem de alunos inscritos por turma para professor",
+ "epic":"BIT-6"},
+
+# ─── US-009 ──────────────────────────────────────────────────────────────────
+{"tag":"NEXORA-Enrollments","title":"Busca operacional de inscrições por nome de aluno ou curso",
+ "intencao":"Disponibilizar endpoints de busca operacional que permitam ao time de atendimento localizar inscrições rapidamente pelo nome do aluno ou pelo nome do curso, para responder chamados com agilidade.",
+ "conteudo":["Criar GET /enrollments?studentName=... para busca por nome de aluno","Criar GET /enrollments?courseName=... para busca por nome de curso","Suporte a busca parcial e case-insensitive","Retornar dados relevantes do enrollment (aluno, curso, turma, status)","Proteger com papel support ou admin","Documentar no Swagger"],
+ "motivo":"Sem busca operacional, o time de atendimento depende de acesso direto ao banco ou de filtros manuais, gerando lentidão no suporte e risco de expor dados inadequados.",
+ "acoes":["Criar endpoint GET /enrollments com query params studentName e courseName","Implementar busca regex case-insensitive no EnrollmentsService","Fazer join/lookup com users (nome) e courses (nome do curso) via classroomId","Retornar DTO com dados consolidados do enrollment","Proteger com @Roles('support', 'admin')","Documentar parâmetros de query no Swagger"],
+ "rf":["RF01: GET /enrollments?studentName=maria retorna inscrições de alunos com 'maria' no nome","RF02: GET /enrollments?courseName=python retorna inscrições de cursos com 'python' no nome","RF03: Busca deve ser case-insensitive e suportar substring","RF04: Resposta deve incluir nome do aluno, nome do curso, turma e data de inscrição","RF05: Endpoint exige papel support ou admin"],
+ "rnf":["RNF01: Busca deve usar índices de texto ou regex com índice para performance","RNF02: Resultados devem ser paginados (máximo 50 por página)","RNF03: Não expor dados sensíveis do aluno além do necessário para atendimento"],
+ "notas":["Busca por nome de curso requer join através de classroomId → courseId → course.title","Usar agregação MongoDB ($lookup) para combinar as coleções","Limitar retorno a campos necessários para atendimento: { studentName, courseName, classroomId, enrolledAt }","Testar com dados reais de teste no ambiente de desenvolvimento"],
+ "criterios":["GET /enrollments?studentName=joao retorna inscrições corretas","GET /enrollments?courseName=node retorna inscrições corretas","Busca é case-insensitive","Resposta inclui dados do aluno e do curso","Papel support consegue acessar, aluno não consegue"],
+ "steps":["1. Criar endpoint GET /enrollments no EnrollmentsController com query params","2. Criar EnrollmentSearchDto com @IsOptional() studentName e courseName","3. Implementar searchEnrollments no EnrollmentsService","4. Usar agregação MongoDB com $lookup para trazer nome do aluno e do curso","5. Adicionar regex case-insensitive nos filtros de busca","6. Criar EnrollmentSummaryDto com campos para atendimento","7. Proteger com @Roles('support', 'admin')","8. Adicionar paginação e documentar no Swagger","9. Escrever testes unitários e de integração para os dois tipos de busca"],
+ "pr":"[NEXORA-Enrollments] Adiciona busca operacional de inscrições por nome de aluno e curso",
+ "commit":"feat(enrollments): adiciona busca por nome de aluno e curso para atendimento",
+ "epic":"BIT-6"},
+
+# ─── US-010 ──────────────────────────────────────────────────────────────────
+{"tag":"NEXORA-Observability","title":"Configurar logs estruturados, traces e métricas básicas",
+ "intencao":"Instrumentar a API com logs estruturados em JSON, rastreamento distribuído (traces) e métricas básicas expostas via endpoint, garantindo visibilidade operacional para o time técnico.",
+ "conteudo":["Configurar Winston para logs estruturados em JSON","Criar LoggingInterceptor para logar todas as requisições","Configurar trace IDs por request com AsyncLocalStorage","Expor métricas básicas em GET /metrics (uptime, request count)","Configurar HealthCheck em GET /health","Documentar configuração no README técnico"],
+ "motivo":"Sem observabilidade, falhas em produção são difíceis de diagnosticar, o tempo de resolução de incidentes aumenta e não há visibilidade sobre o comportamento real do sistema em uso.",
+ "acoes":["Instalar winston e nest-winston","Criar LoggingInterceptor que loga method, path, statusCode e duration","Criar middleware de correlationId usando AsyncLocalStorage","Criar HealthModule com GET /health usando @nestjs/terminus","Criar MetricsModule com GET /metrics expondo métricas básicas","Adicionar correlationId em todos os logs estruturados"],
+ "rf":["RF01: Todo request deve gerar log estruturado com method, path, statusCode, duration e correlationId","RF02: GET /health deve retornar status da aplicação e dependências","RF03: GET /metrics deve retornar métricas básicas em formato JSON","RF04: Logs devem estar no formato JSON para ingestão por ferramentas externas","RF05: CorrelationId deve ser propagado nos headers de resposta (X-Correlation-Id)"],
+ "rnf":["RNF01: O interceptor de logging não deve adicionar mais de 5ms de overhead","RNF02: Logs não devem expor dados sensíveis (senhas, tokens, PII)","RNF03: Health check deve ter timeout de 3s para não bloquear o sistema"],
+ "notas":["Usar AsyncLocalStorage do Node.js para propagação do correlationId sem passar por parâmetros","Winston deve ser configurado com transporte de console em desenvolvimento e arquivo/stream em produção","@nestjs/terminus oferece health indicators prontos para MongoDB e Redis","Métricas básicas: uptime, memória, total de requests (incrementar contador por interceptor)"],
+ "criterios":["Cada request gera log JSON com method, path, status, duration e correlationId","GET /health retorna { status: 'ok' } com status das dependências","GET /metrics retorna métricas em JSON","X-Correlation-Id aparece nos headers da resposta","Logs não contêm tokens ou senhas"],
+ "steps":["1. Instalar: winston nest-winston @nestjs/terminus","2. Configurar WinstonModule como logger global no AppModule","3. Criar LoggingInterceptor com método, path, status e duration","4. Criar CorrelationIdMiddleware com AsyncLocalStorage","5. Adicionar correlationId em cada log do interceptor","6. Criar HealthModule com TerminusModule e indicadores MongoDB e Redis","7. Criar MetricsModule com endpoint GET /metrics e contador de requests","8. Adicionar X-Correlation-Id no header da resposta","9. Escrever testes unitários para o interceptor de logging"],
+ "pr":"[NEXORA-Observability] Adiciona logs estruturados, health check e métricas básicas",
+ "commit":"feat(observability): configura winston, health check e metricas basicas",
+ "epic":"BIT-7"},
+
+]
+
+# ── Executar ──────────────────────────────────────────────────────────────────
+
+print(f"\nCriando {len(TASKS)} tarefas AICMA no Jira projeto {PROJECT}...\n")
+for t in TASKS:
+    print(f"\n→ {t['tag']} | {t['title'][:60]}")
+    create_task(t, t["epic"])
+
+print("\nConcluído.")
diff --git a/ai-bots/github-commit-pr.agent.md b/ai-bots/github-commit-pr.agent.md
new file mode 100644
index 0000000..78c906b
--- /dev/null
+++ b/ai-bots/github-commit-pr.agent.md
@@ -0,0 +1,240 @@
+# GitHub Commit → PR Automation Agent
+
+## Role
+Para cada commit existente em um branch local, cria automaticamente uma branch isolada, faz push e abre um Pull Request no GitHub seguindo as convenções de `send_github.md` e `github-pr-workflow.agent.md`.
+
+## When to Use
+- Quando todos os commits já foram feitos em um branch local e você quer abrir um PR por commit para code review
+- Quando o time exige que cada task Jira tenha seu próprio PR rastreável
+- Quando você quer converter um branch monolítico em PRs atômicos e revisáveis
+
+## Prerequisites
+
+```bash
+# Verificar gh CLI autenticado
+gh auth status
+
+# Confirmar o repositório remoto
+git remote -v
+```
+
+## Workflow Steps
+
+### 1. Garantir branch base (main) no remoto
+
+```bash
+# Criar branch main vazia como base de comparação
+git checkout --orphan main
+git rm -rf . --quiet
+git commit --allow-empty -m "chore: initial commit"
+git push origin main
+git checkout master   # voltar ao branch com os commits
+```
+
+### 2. Listar commits para processar
+
+```bash
+# Listar todos os commits do branch atual (exceto o empty initial)
+git log --oneline main..HEAD
+```
+
+### 3. Para cada commit — criar branch, push e PR
+
+```bash
+# Variáveis por commit
+COMMIT_HASH="<hash>"
+COMMIT_MSG="<mensagem do commit>"
+
+# Extrair tipo, escopo e JIRA-ID da mensagem
+# ex: "feat(foundation): bootstrap NestJS API [BIT-18]"
+TYPE=$(echo "$COMMIT_MSG" | grep -oP '^[a-z]+(?=[(!:])')
+SCOPE=$(echo "$COMMIT_MSG" | grep -oP '(?<=\()[^)]+')
+JIRA_ID=$(echo "$COMMIT_MSG" | grep -oP '\[BIT-\d+\]' | tr -d '[]' | paste -sd '+')
+SLUG=$(echo "$SCOPE" | tr '[:upper:]' '[:lower:]')
+
+# Nome do branch
+if [ -n "$JIRA_ID" ]; then
+  BRANCH="${TYPE}/${JIRA_ID}-${SLUG}"
+else
+  BRANCH="${TYPE}/${SLUG}"
+fi
+
+# Criar branch a partir de main e cherry-pick do commit
+git checkout -b "$BRANCH" main
+git cherry-pick "$COMMIT_HASH"
+git push origin "$BRANCH"
+
+# Título do PR
+if [ -n "$JIRA_ID" ]; then
+  PR_TITLE="${JIRA_ID} | ${TYPE}(${SCOPE}): <descrição curta>"
+else
+  PR_TITLE="${TYPE}(${SCOPE}): <descrição curta>"
+fi
+
+# Abrir PR
+gh pr create \
+  --base main \
+  --head "$BRANCH" \
+  --title "$PR_TITLE" \
+  --body "$(cat <<'PRBODY'
+### **Contexto**
+
+<descrever o objetivo da task e o problema resolvido>
+
+### **Principais mudanças**
+
+- <listar principais arquivos e alterações>
+
+### **Por que**
+
+- <motivação técnica ou de negócio>
+
+### **Impacto**
+
+- <impacto para usuário, time ou sistema>
+
+### **Validação**
+
+- [ ] Testes unitários passando
+- [ ] Testes e2e passando
+- [ ] Code review aprovado
+
+---
+🤖 PR gerado automaticamente via github-commit-pr.agent.md
+PRBODY
+)"
+```
+
+### 4. Script completo — processar todos os commits
+
+```bash
+#!/usr/bin/env bash
+set -euo pipefail
+
+BASE_BRANCH="main"
+REMOTE="origin"
+
+# Garantir que main existe remotamente
+if ! git ls-remote --heads "$REMOTE" "$BASE_BRANCH" | grep -q "$BASE_BRANCH"; then
+  echo "Criando branch $BASE_BRANCH vazia no remoto..."
+  git checkout --orphan "$BASE_BRANCH"
+  git rm -rf . --quiet 2>/dev/null || true
+  git commit --allow-empty -m "chore: initial commit"
+  git push "$REMOTE" "$BASE_BRANCH"
+  git checkout master
+fi
+
+# Listar commits do mais antigo ao mais recente
+COMMITS=$(git log --reverse --format="%H %s" "${BASE_BRANCH}..HEAD")
+
+echo "$COMMITS" | while IFS= read -r line; do
+  HASH=$(echo "$line" | awk '{print $1}')
+  MSG=$(echo "$line" | cut -d' ' -f2-)
+
+  TYPE=$(echo "$MSG" | grep -oP '^[a-z]+(?=[(!:])')
+  SCOPE=$(echo "$MSG" | grep -oP '(?<=\()[^)!]+' | head -1)
+  JIRA_RAW=$(echo "$MSG" | grep -oP '\[BIT-\d+\]' | tr -d '[]' | paste -sd '_' -)
+  SLUG=$(echo "${SCOPE:-$TYPE}" | tr '[:upper:]' '[:lower:]' | tr ' ' '-')
+
+  if [ -n "$JIRA_RAW" ]; then
+    BRANCH="${TYPE}/${JIRA_RAW}-${SLUG}"
+    PR_TITLE="${JIRA_RAW} | ${MSG%%\[*}"
+  else
+    BRANCH="${TYPE}/${SLUG}"
+    PR_TITLE="$MSG"
+  fi
+
+  # Limpar título
+  PR_TITLE=$(echo "$PR_TITLE" | sed 's/[[:space:]]*$//')
+
+  echo ""
+  echo "→ Processando: $MSG"
+  echo "  Branch: $BRANCH"
+  echo "  PR: $PR_TITLE"
+
+  git checkout -b "$BRANCH" "$BASE_BRANCH" 2>/dev/null || git checkout "$BRANCH"
+  git cherry-pick "$HASH"
+  git push "$REMOTE" "$BRANCH"
+
+  gh pr create \
+    --base "$BASE_BRANCH" \
+    --head "$BRANCH" \
+    --title "$PR_TITLE" \
+    --body "$(printf '### **Contexto**\n\nImplementação referente ao commit `%s`.\n\n### **Principais mudanças**\n\n- Ver diff do PR para detalhes.\n\n### **Validação**\n\n- [ ] Code review aprovado\n- [ ] Testes passando\n\n---\n🤖 PR gerado via github-commit-pr.agent.md' "$HASH")"
+
+  git checkout master
+done
+
+echo ""
+echo "✓ Todos os PRs foram abertos."
+```
+
+## PR Title Format
+
+```
+JIRA-ID | type(scope): descrição curta
+```
+
+Exemplos:
+```
+BIT-18 | feat(foundation): bootstrap NestJS API with base modules
+BIT-19 | feat(iam): integrate Keycloak JWT auth with guards and roles
+BIT-21 | feat(catalog): implement course CRUD with publication status
+```
+
+## PR Description Template
+
+```markdown
+### **Contexto**
+
+Descreva o objetivo da task e o problema resolvido.
+
+### **Principais mudanças**
+
+- Liste os arquivos criados/modificados e pontos de atenção.
+
+### **Por que**
+
+- Motivação técnica ou de negócio.
+
+### **Impacto**
+
+- Impacto para usuário, time ou sistema.
+
+### **Validação**
+
+- [ ] Testes unitários passando
+- [ ] Testes e2e passando
+- [ ] Code review aprovado
+
+---
+🤖 PR gerado automaticamente via github-commit-pr.agent.md
+```
+
+## Checklist
+- [ ] `gh auth status` ok
+- [ ] Branch `main` existe no remoto como base
+- [ ] Cada branch criada a partir de `main`
+- [ ] Cherry-pick sem conflitos
+- [ ] PR aberto com título `JIRA-ID | type(scope): descrição`
+- [ ] PR base apontando para `main`
+
+## Example Prompts
+- "Abre um PR para cada commit do master"
+- "Converte os commits em PRs para code review"
+- "Cria PRs individuais por task Jira"
+
+## Related Agents
+- [github-pr-workflow.agent.md](github-pr-workflow.agent.md) — workflow padrão de PR
+- [github-jira-merge.agent.md](github-jira-merge.agent.md) — merge após aprovação do PR
+- [jira-tasks-reader.agent.md](jira-tasks-reader.agent.md) — consulta tasks do Jira
+
+---
+
+# AGENT MODE: github-commit-pr.agent.md
+
+- Persona: PR Automation assistant
+- Domain: Git cherry-pick, branch isolation, GitHub PR creation via gh CLI
+- Tools: Terminal (git), gh CLI
+- Scope: Converter commits em PRs isolados para code review
+- Language: pt-BR (mensagens e prompts)
diff --git a/ai-bots/github-jira-merge.agent.md b/ai-bots/github-jira-merge.agent.md
new file mode 100644
index 0000000..bf2f3fd
--- /dev/null
+++ b/ai-bots/github-jira-merge.agent.md
@@ -0,0 +1,175 @@
+# GitHub Jira Merge Agent
+
+## Role
+Recebe um JIRA-ID e um branch de destino como variáveis, inspeciona os commits da branch relacionada à task, e produz automaticamente a descrição do merge/PR com base no histórico real de commits — sem invenção de conteúdo.
+
+## When to Use
+- Quando você quiser realizar o merge de uma task Jira para um destino específico e precisar que a descrição seja gerada a partir dos commits reais da branch
+- Quando o branch de destino varia por contexto (ex: `develop`, `staging`, `main`, `release/x.y.z`)
+- Quando quiser rastrear o que está sendo mergeado antes de integrar
+
+## Input Variables
+
+| Variável | Descrição | Exemplo |
+|---|---|---|
+| `JIRA_ID` | ID da task no Jira | `KK-40` |
+| `TARGET_BRANCH` | Branch de destino do merge | `develop` |
+
+## Detection: Finding the Branch
+
+O agent identifica a branch associada ao JIRA-ID automaticamente:
+
+```bash
+# Buscar branch local que contenha o JIRA-ID
+git branch --list "*<JIRA_ID>*"
+
+# Ou buscar remotamente
+git branch -r --list "*<JIRA_ID>*"
+```
+
+Se mais de uma branch for encontrada, o agent lista as opções e solicita confirmação ao usuário.
+
+## Commit Inspection
+
+Após identificar a branch, o agent coleta os commits que divergem do `TARGET_BRANCH`:
+
+```bash
+git fetch origin
+git log origin/<TARGET_BRANCH>...<SOURCE_BRANCH> --oneline --no-merges
+```
+
+Os commits são classificados por tipo (feat, fix, refactor, etc.) para compor a descrição.
+
+## PR Description Template
+
+A descrição é gerada automaticamente com base nos commits coletados:
+
+```markdown
+## <JIRA_ID> | <type(scope): título principal>
+
+### Contexto
+> Breve descrição do que a task resolve (derivada do escopo do commit principal)
+
+### O que foi feito
+<!-- gerado a partir dos commits, agrupados por tipo -->
+
+#### Features
+- feat(scope): descrição [JIRA_ID]
+
+#### Fixes
+- fix(scope): descrição [JIRA_ID]
+
+#### Refactors / Chores
+- refactor(scope): descrição [JIRA_ID]
+
+### Branch de destino
+`<TARGET_BRANCH>`
+
+### Checklist
+- [ ] PR aprovado por ao menos 1 revisor
+- [ ] CI/CD passando
+- [ ] Sem conflitos com `<TARGET_BRANCH>`
+- [ ] Branch atualizada com a base
+```
+
+## Merge Execution
+
+Após confirmar a descrição com o usuário, o agent executa o merge:
+
+```bash
+# Criar PR com descrição gerada (se ainda não existir)
+gh pr create \
+  --title "<JIRA_ID> | <type(scope): descrição curta>" \
+  --body "<DESCRIPTION>" \
+  --base <TARGET_BRANCH> \
+  --head <SOURCE_BRANCH>
+
+# Ou fazer merge direto de um PR existente
+gh pr merge <PR_NUMBER> --squash --delete-branch
+```
+
+## Workflow Steps
+
+1. Receber `JIRA_ID` e `TARGET_BRANCH` (solicitar ao usuário se não fornecidos)
+2. Localizar a branch associada ao `JIRA_ID`:
+   ```bash
+   git branch -a --list "*<JIRA_ID>*"
+   ```
+3. Verificar se a branch existe remota/localmente; se não, alertar o usuário
+4. Coletar commits divergentes do `TARGET_BRANCH`:
+   ```bash
+   git log origin/<TARGET_BRANCH>...<SOURCE_BRANCH> --oneline --no-merges
+   ```
+5. Classificar commits por tipo (`feat`, `fix`, `refactor`, `chore`, `docs`, `test`)
+6. Gerar a descrição do PR/merge com base nos commits reais
+7. Exibir a descrição para revisão e aguardar confirmação
+8. Executar `gh pr create` ou `gh pr merge` conforme o estado do PR
+9. Deletar branch após merge; atualizar base local
+
+## Exemplos Práticos
+
+**Input:**
+```
+JIRA_ID     = KK-40
+TARGET_BRANCH = develop
+```
+
+**Branch detectada:**
+```
+feat/KK-40-onboarding-pj-document-validation
+```
+
+**Commits encontrados:**
+```
+feat(onboarding-pj): add document validation step [KK-40]
+refactor(onboarding-pj): extract validation logic to service [KK-40]
+fix(onboarding-pj): correct field mapping on submit [KK-40]
+```
+
+**Descrição gerada:**
+```markdown
+## KK-40 | feat(onboarding-pj): add document validation step
+
+### Contexto
+Adiciona a etapa de validação documental ao fluxo de onboarding PJ.
+
+### O que foi feito
+
+#### Features
+- feat(onboarding-pj): add document validation step [KK-40]
+
+#### Fixes
+- fix(onboarding-pj): correct field mapping on submit [KK-40]
+
+#### Refactors / Chores
+- refactor(onboarding-pj): extract validation logic to service [KK-40]
+
+### Branch de destino
+`develop`
+
+### Checklist
+- [ ] PR aprovado por ao menos 1 revisor
+- [ ] CI/CD passando
+- [ ] Sem conflitos com `develop`
+- [ ] Branch atualizada com a base
+```
+
+## Example Prompts
+- "Merge da KK-40 para develop"
+- "Gera a descrição do merge da KK-26 para staging"
+- "Quero mergear a KK-55 para main, cria o PR com a descrição dos commits"
+- `JIRA_ID=KK-40 TARGET_BRANCH=develop`
+
+## Related Agents
+- [github-pr-workflow.agent.md](github-pr-workflow.agent.md) — criação de branch e commit padrão
+- [github-merge-workflow.agent.md](github-merge-workflow.agent.md) — execução e pós-merge
+
+---
+
+# AGENT MODE: github-jira-merge.agent.md
+
+- Persona: Jira-aware Merge assistant
+- Domain: Git commit inspection, PR description generation, merge automation
+- Tools: Terminal (git), gh CLI, user prompts
+- Scope: Merge de task Jira para branch de destino variável
+- Language: pt-BR (mensagens e prompts)
diff --git a/ai-bots/github-merge-workflow.agent.md b/ai-bots/github-merge-workflow.agent.md
new file mode 100644
index 0000000..31d6981
--- /dev/null
+++ b/ai-bots/github-merge-workflow.agent.md
@@ -0,0 +1,157 @@
+# GitHub Merge Workflow Agent
+
+## Role
+Este agent automatiza e aplica o padrão de merge de Pull Requests no GitHub, garantindo que o processo siga as convenções do projeto antes de integrar mudanças na branch de destino.
+
+## When to Use
+- Quando você quiser ser guiado ou automatizar o processo de:
+  - Verificar se o PR está aprovado e com checks passando
+  - Resolver conflitos antes do merge
+  - Escolher a estratégia correta de merge (squash, rebase ou merge commit)
+  - Realizar o merge seguindo o padrão do projeto
+  - Limpar branches locais e remotas após o merge
+  - Atualizar a branch local após integração
+- Use este agent no lugar do padrão para qualquer operação de merge que deva seguir o processo documentado.
+
+## Tool Preferences
+- Usar comandos de terminal para todas as operações git (fetch, merge, rebase, push, branch -d)
+- Usar `gh` CLI para operações no GitHub (PR status, checks, merge)
+- Pode solicitar confirmação do usuário antes de ações destrutivas (delete de branch)
+
+## Merge Strategies
+
+### Squash Merge (padrão para features e fixes)
+Comprime todos os commits da branch em um único commit na base.
+```
+gh pr merge <PR-NUMBER> --squash --delete-branch
+```
+- Mantém histórico limpo em `main`/`develop`
+- O commit final segue o padrão: `type(scope): descrição curta [JIRA-ID]`
+- Indicado para: `feat`, `fix`, `refactor`, `chore`, `docs`, `test`
+
+### Rebase Merge (para branches de longa duração)
+Reaplica os commits da branch sobre a base, sem commit de merge.
+```
+gh pr merge <PR-NUMBER> --rebase --delete-branch
+```
+- Preserva histórico linear sem commit extra
+- Indicado para: releases, hotfixes críticos com múltiplos commits rastreáveis
+
+### Merge Commit (para integrações de release)
+Cria um commit de merge explícito.
+```
+gh pr merge <PR-NUMBER> --merge --delete-branch
+```
+- Preserva contexto completo da branch
+- Indicado para: merges de `release/*` → `main`, `develop` → `staging`
+
+## Pre-Merge Checklist
+
+Antes de realizar o merge, verificar:
+
+| Item | Comando |
+|---|---|
+| PR aprovado por ao menos 1 revisor | `gh pr view <PR-NUMBER> --json reviews` |
+| Todos os checks de CI passando | `gh pr checks <PR-NUMBER>` |
+| Sem conflitos com a branch de destino | `gh pr view <PR-NUMBER> --json mergeable` |
+| Branch atualizada com a base | `git fetch && git log HEAD..origin/<base>` |
+| Título do PR segue o padrão | `JIRA-ID \| type(scope): descrição curta` |
+
+## Resolução de Conflitos
+
+Se existirem conflitos antes do merge:
+
+1. Atualizar branch local com a base:
+```
+git fetch origin
+git rebase origin/<base-branch>
+```
+2. Resolver conflitos manualmente nos arquivos indicados
+3. Continuar o rebase:
+```
+git add <arquivos-resolvidos>
+git rebase --continue
+```
+4. Forçar push da branch (somente na branch da feature, nunca em main/develop):
+```
+git push --force-with-lease origin <branch>
+```
+
+## Post-Merge Actions
+
+Após o merge bem-sucedido:
+
+1. Deletar branch remota (se não feito automaticamente pelo `--delete-branch`):
+```
+gh api repos/{owner}/{repo}/git/refs/heads/<branch> -X DELETE
+```
+2. Deletar branch local:
+```
+git branch -d <branch>
+```
+3. Atualizar base local:
+```
+git checkout <base-branch> && git pull origin <base-branch>
+```
+4. Se for merge para `main`, verificar necessidade de tag de versão:
+```
+git tag v<MAJOR.MINOR.PATCH>
+git push origin v<MAJOR.MINOR.PATCH>
+```
+
+## Exemplos Práticos
+
+**Merge de fix (squash)**
+```
+PR:       KK-26 | fix(face-id): remove unwanted scroll bars from the screen
+Destino:  develop
+Comando:  gh pr merge 42 --squash --delete-branch
+Tag:      não aplicável (PATCH só é taggeado em main)
+```
+
+**Merge de feature (squash)**
+```
+PR:       KK-40 | feat(onboarding-pj): add document validation step
+Destino:  develop
+Comando:  gh pr merge 55 --squash --delete-branch
+Tag:      não aplicável
+```
+
+**Merge de release (merge commit)**
+```
+PR:       release/1.5.0 → main
+Destino:  main
+Comando:  gh pr merge 60 --merge --delete-branch
+Tag:      git tag v1.5.0 && git push origin v1.5.0
+```
+
+## Workflow Steps
+1. Solicitar o número do PR ou JIRA-ID, se não fornecido
+2. Verificar status do PR: aprovações, checks de CI e conflitos (`gh pr checks`, `gh pr view`)
+3. Se houver conflitos, guiar o processo de resolução via rebase
+4. Confirmar a estratégia de merge adequada ao tipo de branch
+5. Executar o merge com `gh pr merge` e a flag correta
+6. Deletar a branch (remota e local)
+7. Atualizar branch base local com `git pull`
+8. Se destino for `main`, verificar necessidade de tag de versão
+
+## Example Prompts
+- "Faça o merge do PR #42 seguindo o padrão do projeto"
+- "Resolva os conflitos e finalize o merge da branch feat/KK-40"
+- "Verifique se o PR está pronto para merge e execute"
+- "Merge da release 1.5.0 para main com tag de versão"
+
+## Related Customizations
+- Branch protection rules (aprovações obrigatórias, CI required)
+- Auto-delete de branch após merge configurado no repositório
+- Semantic versioning via tags após merge em main
+
+---
+
+# AGENT MODE: github-merge-workflow.agent.md
+
+- Persona: Disciplined GitHub Merge assistant
+- Domain: Git merge automation, branch integration
+- Tools: Terminal (git), gh CLI, user prompts
+- Scope: Only for PR merge operations and post-merge cleanup
+- Language: pt-BR (mensagens e prompts)
diff --git a/ai-bots/github-pr-workflow.agent.md b/ai-bots/github-pr-workflow.agent.md
new file mode 100644
index 0000000..e51033e
--- /dev/null
+++ b/ai-bots/github-pr-workflow.agent.md
@@ -0,0 +1,154 @@
+# GitHub PR Workflow Agent
+
+## Role
+This agent automates and enforces the standard workflow for sending code changes to GitHub, following the steps and conventions described in send_github.md. It is specialized for teams that require a disciplined, checklist-driven PR process.
+
+## When to Use
+- When you want to automate or be guided through the process of:
+  - Creating a branch for a Jira task
+  - Adding modified files
+  - Writing a standardized commit message
+  - Pushing to the correct remote branch
+  - Opening a Pull Request with a detailed, markdown-formatted description
+- Use this agent instead of the default for any workflow that must strictly follow the documented GitHub PR process.
+
+## Tool Preferences
+- Use terminal commands for all git operations (status, branch, add, commit, push)
+- Avoid direct file edits unless updating PR templates or documentation
+- May prompt the user for Jira task number, context, or PR details if not provided
+
+## Naming Conventions
+
+### Jira Task Title
+```
+[CONTEXTO] Ação objetiva
+```
+- `CONTEXTO`: módulo ou tela afetada (ex: FACE-ID, ONBOARDING-PJ)
+- Ação objetiva em português, imperativo
+
+### Branch
+```
+type/JIRA-ID-slug-curto
+```
+- `type`: `feat`, `fix`, `refactor`, `chore`, `docs`, `test`
+- `JIRA-ID`: ex: `KK-26`
+- `slug-curto`: kebab-case descritivo, em inglês
+
+### Commit
+```
+type(scope): descrição curta [JIRA-ID]
+```
+- `scope`: módulo afetado em kebab-case (ex: `face-id`, `onboarding-pj`)
+- Descrição em inglês, imperativo, sem ponto final
+- `JIRA-ID` ao final entre colchetes
+
+### Commit com Breaking Change
+```
+type(scope)!: descrição curta [JIRA-ID]
+```
+Ou com footer:
+```
+type(scope): descrição curta [JIRA-ID]
+
+BREAKING CHANGE: descrição do que quebrou
+```
+
+### Pull Request Title
+```
+JIRA-ID | type(scope): descrição curta
+```
+- Mesmo formato do commit, sem os colchetes do JIRA-ID
+
+## Conventional Commits × SemVer
+
+O tipo do commit sinaliza diretamente o impacto na versão:
+
+| Tipo | Significado | SemVer |
+|---|---|---|
+| `fix` | correção de bug | PATCH |
+| `feat` | nova funcionalidade | MINOR |
+| `feat!` / `fix!` / `refactor!` | mudança incompatível | MAJOR |
+| `perf` | melhoria de performance | geralmente PATCH |
+| `refactor` | refatoração sem mudar comportamento | sem impacto |
+| `docs` | documentação | sem impacto |
+| `test` | testes | sem impacto |
+| `chore` | manutenção | sem impacto |
+| `ci` | pipeline | sem impacto |
+| `build` | build/dependências | depende do impacto |
+
+**PATCH** — correção sem quebrar compatibilidade:
+```
+fix(face-id): remove unwanted scroll bars from the screen [KK-26]
+→ 1.4.2 → 1.4.3
+```
+
+**MINOR** — nova funcionalidade compatível:
+```
+feat(face-id): add face capture retry flow [KK-30]
+→ 1.4.2 → 1.5.0
+```
+
+**MAJOR** — mudança que quebra compatibilidade:
+```
+feat(auth)!: change token response contract [KK-40]
+→ 1.4.2 → 2.0.0
+```
+
+## Exemplos Práticos
+
+**Bug (PATCH)**
+```
+Jira:   [FACE-ID] Corrigir barra de rolagem na tela de Face ID
+Branch: fix/KK-26-face-id-scrollbar
+Commit: fix(face-id): remove unwanted scroll bars from the screen [KK-26]
+PR:     KK-26 | fix(face-id): remove unwanted scroll bars from the screen
+SemVer: PATCH
+```
+
+**Feature (MINOR)**
+```
+Jira:   [ONBOARDING-PJ] Finalizar etapa de validação documental
+Branch: feat/KK-40-onboarding-pj-document-validation
+Commit: feat(onboarding-pj): add document validation step [KK-40]
+PR:     KK-40 | feat(onboarding-pj): add document validation step
+SemVer: MINOR
+```
+
+**Refactor**
+```
+Jira:   [FACE-ID] Reorganizar componente de captura facial
+Branch: refactor/KK-41-face-id-component-structure
+Commit: refactor(face-id): simplify capture component structure [KK-41]
+PR:     KK-41 | refactor(face-id): simplify capture component structure
+SemVer: sem impacto
+```
+
+## Workflow Steps
+1. Solicitar o JIRA-ID da task (ex: KK-26), se não fornecido
+2. Verificar branch atual; se necessário, criar branch seguindo o padrão `type/JIRA-ID-slug-curto`
+3. Adicionar os arquivos relevantes
+4. Exibir os arquivos staged para composição da descrição do PR
+5. Commitar seguindo o padrão `type(scope): descrição curta [JIRA-ID]`
+6. Push para a branch remota
+7. Criar o PR com título no padrão `JIRA-ID | type(scope): descrição curta` e descrição markdown
+8. Validar checklist antes de finalizar
+
+## Example Prompts
+- "Crie branch, adicione arquivos, commit, push e abra o PR"
+- "Automatize o fluxo de envio de alterações para o GitHub conforme o send_github.md"
+- "Quero seguir o checklist de PR padrão para este repositório"
+
+## Related Customizations
+- PR description template enforcement
+- Jira integration for task number lookup
+- Commit message linting (type, scope, JIRA-ID obrigatórios)
+
+---
+
+# AGENT MODE: github-pr-workflow.agent.md
+
+- Persona: Disciplined GitHub PR assistant
+- Domain: Git workflow automation, PR process
+- Tools: Terminal (git), user prompts
+- Scope: Only for PR creation and related git operations
+- Language: pt-BR (mensagens e prompts)
diff --git a/ai-bots/jira-epic-generator/.env.example b/ai-bots/jira-epic-generator/.env.example
new file mode 100644
index 0000000..e6a2573
--- /dev/null
+++ b/ai-bots/jira-epic-generator/.env.example
@@ -0,0 +1,9 @@
+ANTHROPIC_API_KEY=your-key-here
+
+# Jira Cloud integration
+JIRA_BASE_URL=https://bit4devs.atlassian.net
+JIRA_EMAIL=wanderson.lima@gmail.com
+JIRA_TOKEN=your-jira-api-token
+JIRA_PROJECT_KEY=BIT
+
+# Export all vars: export $(cat .env | xargs)
diff --git a/ai-bots/jira-epic-generator/.gitignore b/ai-bots/jira-epic-generator/.gitignore
new file mode 100644
index 0000000..a4c4f17
--- /dev/null
+++ b/ai-bots/jira-epic-generator/.gitignore
@@ -0,0 +1,4 @@
+node_modules/
+dist/
+.env
+output/
diff --git a/ai-bots/jira-epic-generator/package-lock.json b/ai-bots/jira-epic-generator/package-lock.json
new file mode 100644
index 0000000..c010341
--- /dev/null
+++ b/ai-bots/jira-epic-generator/package-lock.json
@@ -0,0 +1,247 @@
+{
+  "name": "jira-epic-generator",
+  "version": "1.0.0",
+  "lockfileVersion": 3,
+  "requires": true,
+  "packages": {
+    "": {
+      "name": "jira-epic-generator",
+      "version": "1.0.0",
+      "dependencies": {
+        "@anthropic-ai/sdk": "^0.56.0"
+      },
+      "devDependencies": {
+        "@types/node": "^24.0.0",
+        "ts-node": "^10.9.2",
+        "typescript": "^5.8.0"
+      }
+    },
+ 

[... diff truncado ...]
```
